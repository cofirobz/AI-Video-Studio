# characters/

One YAML file per character, matching `core.models.character.Character`. Created and read via `core.character_repository.CharacterRepository`.

Create one with `python -m core.cli new-character --name "..."`.
