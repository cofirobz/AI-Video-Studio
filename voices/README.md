# voices/

Reference voice samples per character, used for voice cloning/TTS voice selection (see `Character.voice.reference_id`), reused across every episode that character appears in.

TTS output generated *for one specific episode* lives in that episode's own `episodes/{id}/assets/` instead — see `docs/architecture.md`.
